# Personal photo — keep local only, never push to public repo
photo.jpg
Profile Photo.jpg

# Photo-containing resume PDF — keep local only (contains personal photo)
NARASIMHARAO_RESUME_V2_PHOTO.pdf

# LaTeX build artifacts
*.aux
*.log
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
*.toc
